import { Navigate } from 'react-router-dom';

// Redirect to dynamic service page
const MetalRoofInstallation = () => {
  return <Navigate to="/services/metal-roof-installation" replace />;
};

export default MetalRoofInstallation;
