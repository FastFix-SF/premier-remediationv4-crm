import { Navigate } from 'react-router-dom';

// Redirect to dynamic service page
const MetalRoofPanels = () => {
  return <Navigate to="/services/metal-roof-panels" replace />;
};

export default MetalRoofPanels;
