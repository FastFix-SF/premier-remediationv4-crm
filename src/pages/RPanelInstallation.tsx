import { Navigate } from 'react-router-dom';

// Redirect to dynamic service page
const RPanelInstallation = () => {
  return <Navigate to="/services/r-panel-installation" replace />;
};

export default RPanelInstallation;
