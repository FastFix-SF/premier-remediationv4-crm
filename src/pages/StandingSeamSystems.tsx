import { Navigate } from 'react-router-dom';

// Redirect to dynamic service page
const StandingSeamSystems = () => {
  return <Navigate to="/services/standing-seam-systems" replace />;
};

export default StandingSeamSystems;
