import { Navigate } from 'react-router-dom';

// Redirect to dynamic service page
const StormFireEnergy = () => {
  return <Navigate to="/services/storm-fire-energy" replace />;
};

export default StormFireEnergy;
